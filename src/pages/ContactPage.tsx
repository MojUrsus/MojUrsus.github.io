import { Box } from '@chakra-ui/react';

const ContactPage = () => {
    return <Box
        id='contact-page'
        color={'white'}
        bg='gray.100'
        h={'full'}>
        ContactPage
    </Box>;
};

export default ContactPage;